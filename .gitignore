# dependencies
node_modules

# environment files
.env
.env.local
.env.development.local
.env.production.local

# nested env files
backend/.env
frontend/.env
frontend/.env.local
openclaw/.env

# build files
dist
build
coverage

# OS junk
.DS_Store
Thumbs.db

# logs
*.log

# Per-user SOUL context files (personal data — never commit)
openclaw/souls/user-*.md

# Local Supabase migrations (run in dashboard, not version controlled)
supabase_migration.sql
supabase_user_domains.sql